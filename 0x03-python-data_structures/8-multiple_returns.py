#!/usr/bin/python3
def multiple_returns(sentence):
    """function that returns a tuple with the length of a string and its first character."""
    if len(sentence) == 0:
       return (None)

    firstLet = sentence[0]
      for i in range(len(sentence):
          if sentence[i] < firstLet:
            firstLet = my_list[i]

      return (firstLet)
